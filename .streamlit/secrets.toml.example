# Copia este archivo como secrets.toml (sin el .example) y pon tu API key real.
# NUNCA subas secrets.toml a GitHub — está en .gitignore.
#
# Cómo conseguir la API key gratis:
# 1. Ve a https://aistudio.google.com/
# 2. Inicia sesión con una cuenta Google
# 3. Click en "Get API key" → "Create API key in new project"
# 4. Copia la key y pégala abajo

GEMINI_API_KEY = "AIza..."
