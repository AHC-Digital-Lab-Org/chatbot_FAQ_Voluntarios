name: 🚀 CI/CD Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  call-ops-workflow:
    # Workflow reutilizable central de la organización (asociacion-ops).
    uses: AHC-Digital-Lab-Org/asociacion-ops/.github/workflows/streamlit-reusable.yml@main
    with:
      language: 'python'
      python_version: '3.13.11'
    secrets: inherit
